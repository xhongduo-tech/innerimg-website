# 复制为 .env 后由 docker compose 自动加载（可选）
# cp .env.compose.example .env

# 浏览器访问地址（生成图片完整 URL 用，必须与用户实际打开的入口一致）
# 本机：http://localhost:3000
# 内网离线部署：填服务器 IP/域名 + 端口，例如 http://10.0.0.5:3000
PUBLIC_BASE_URL=http://localhost:3000

# 本机映射端口（Nginx）
PORT=3000
