# 对外访问的根 URL（生成图片完整地址时用，勿尾斜杠）
PUBLIC_BASE_URL=https://img.example.com

PORT=3000
HOST=0.0.0.0

# 上传与数据目录（默认可不写）
# UPLOAD_DIR=./data/uploads
# DB_PATH=./data/innerimg.db

MAX_FILE_MB=20
MAX_BATCH_FILES=200

# 全局限速（单 IP）
RATE_LIMIT_MAX=200
RATE_LIMIT_WINDOW=1 minute
